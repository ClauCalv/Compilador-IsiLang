programa
declare a,b,c,d,e,f.
escreva("Programa Teste").
escreva ("Digite A").
leia (e).
a := num(e).
escreva ("Digite B").
leia (f).
b := num(f).
se (a<b) entao
{
 c := a + b.
 c := c + 1.
}senao
{
 c := a - b.
 c := c + 1.
}.
escreva ("C e igual a ").
escreva (c).
d := c * a + b.
escreva ("D e igual a ").
escreva (d).
fimprog.